SET TERM ^ ;
CREATE PROCEDURE MP_SHAREDFILEFOLDERS_INSERT (
    MODULEID Integer,
    FOLDERNAME Varchar(255),
    PARENTID Integer,
    MODULEGUID Char(36),
    FOLDERGUID Char(36),
    PARENTGUID Char(36) )
RETURNS (
    FOLDERID Integer )
AS
BEGIN EXIT; END^
SET TERM ; ^

SET TERM ^ ;
CREATE PROCEDURE MP_SHAREDFILESHISTORY_INSERT (
    ITEMID Integer,
    MODULEID Integer,
    FRIENDLYNAME Varchar(255),
    ORIGINALFILENAME Varchar(255),
    SERVERFILENAME Varchar(50),
    SIZEINKB Integer,
    UPLOADDATE Timestamp,
    ARCHIVEDATE Timestamp,
    UPLOADUSERID Integer,
    ITEMGUID Char(36),
    MODULEGUID Char(36),
    USERGUID Char(36) )
RETURNS (
    ID Integer )
AS
BEGIN EXIT; END^
SET TERM ; ^

SET TERM ^ ;
CREATE PROCEDURE MP_SHAREDFILES_INSERT (
    MODULEID Integer,
    UPLOADUSERID Integer,
    FRIENDLYNAME Varchar(255),
    ORIGINALFILENAME Varchar(255),
    SERVERFILENAME Varchar(255),
    SIZEINKB Integer,
    UPLOADDATE Timestamp,
    FOLDERID Integer,
    ITEMGUID Char(36),
    MODULEGUID Char(36),
    USERGUID Char(36),
    FOLDERGUID Char(36) )
RETURNS (
    ITEMID Integer )
AS
BEGIN EXIT; END^
SET TERM ; ^
