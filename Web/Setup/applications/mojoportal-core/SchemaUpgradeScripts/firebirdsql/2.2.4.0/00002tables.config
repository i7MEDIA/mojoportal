CREATE TABLE mp_BannedIPAddresses (
 RowID INTEGER NOT NULL , 
 BannedIP VARCHAR(50) NOT NULL,
 BannedUTC TIMESTAMP NOT NULL,
 BannedReason VARCHAR(255) ,
PRIMARY KEY (RowID)
);

CREATE SEQUENCE mp_BannedIPAddresses_seq;

