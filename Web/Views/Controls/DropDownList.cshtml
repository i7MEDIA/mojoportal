@model mojoPortal.Web.UI.Razor.DropDownListModel

@if (!string.IsNullOrWhiteSpace(Model.DefaultOption))
{
	@Html.DropDownList(Model.ID, Model.Items, Model.DefaultOption, Model.Attributes)

}
else
{
	@Html.DropDownList(Model.ID, Model.Items, Model.Attributes)
}