You recently requested approval for draft changes made to {ModuleTitle}. Approval was requested on {ApprovalRequestedDate}.

Unfortunately, the changes were rejected by {RejectedBy}.

These comments made by {RejectedBy} may help you to alter the content in order to gain approval once you resubmit it for approval:

"{RejectionReason}"