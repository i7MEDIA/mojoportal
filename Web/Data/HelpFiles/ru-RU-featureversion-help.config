<p><span id="result_box" class="long_text"><span style="background-color: #ebeff9;" title="Note that this is not a complete list of installed features.">Заметим, что это не полный список установленных компонентов. </span><span style="background-color: #fff;" title="It only shows features that have their own tables in the database (ie their own database schema).">Это только список функций, которые имеют свои собственные таблицы в базе данных (т.е. их собственных схем баз данных). </span><span style="background-color: #fff;" title="Some features require no additional tables and therefore have no schema version.">Некоторые функции не требуют дополнительных таблиц и поэтому не имеют версию схемы. </span><span style="background-color: #fff;" title="Whenever a change is made to the database schema for a feature (ie whenever an upgrade script is run for the feature) its schema version is incremented.">Всякий раз, когда вносится изменение в схему базы данных для функции (например, когда сценарий обновления для запуска функции) увеличивается свою версию схемы.</span></span></p>