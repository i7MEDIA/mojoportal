<p>Enter each header on its own line as "HeaderName: header value"</p>

<p>This setting is handy for various SMTP related tasks and debugging. PostmarkApp, for instance, allows you to use SMTP headers for setting which "stream" to use when sending messages.</p>

