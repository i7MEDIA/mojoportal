<p>This settings is only applicable to skins that have this control:</p>
<p>&lt;portal:PageTitle id="PageTitle1" runat="server" /&gt;</p>
<p>If Page Heading is populated the above control will render it, otherwise it will render the page title override if it is not empty, otherwise the page name.</p>
