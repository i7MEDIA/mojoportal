<p>If checked and not showing the option for Html vs Plain text, the subscription will be created with the preference for Html, otherwise it will be created with the preference for Plain Text.</p>
<p>If you show the option for the user to select, by default it will select the Html option if Html is the default, otherwise it will select the plain text option by default.</p>